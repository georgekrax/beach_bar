query Cart($cartId: ID) {
  cart(cartId: $cartId) {
    id
    total
    products {
      ...CartProductBase
    }
    foods {
      ...CartFoodBase
    }
    notes {
      ...CartNoteBase
    }
  }
}
