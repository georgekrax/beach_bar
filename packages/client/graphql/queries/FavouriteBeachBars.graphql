query FavouriteBeachBars($limit: Int) {
  favouriteBeachBars(limit: $limit) {
    beachBar {
      id
      name
      slug
      thumbnailUrl
      # location {
      #   formattedLocation
      # }
    }
  }
}
