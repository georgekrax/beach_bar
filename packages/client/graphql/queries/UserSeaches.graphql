query UserSearches($limit: Int) {
  userSearches(limit: $limit) {
    id
    date
    adults
    children
    inputValue {
      id
      city {
        id
        name
      }
      region {
        id
        name
      }
      beachBar {
        id
        name
        thumbnailUrl
      }
    }
  }
}
