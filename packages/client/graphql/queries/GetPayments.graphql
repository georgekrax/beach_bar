query GetPayments($monthId: ID, $year: Int) {
	getPayments(monthId: $monthId, year: $year) {
		beachBar {
			id
			name
			thumbnailUrl
			location {
				city {
					name
				}
				region {
					name
				}
			}
		}
		visits {
			isUpcoming
			isRefunded
			date
			time {
				id
				value
			}
			payment {
				id
				refCode
			}
		}
	}
}