mutation UpdateFavouriteBeachBar($slug: ID!) {
  updateFavouriteBeachBar(slug: $slug) {
    id
    beachBar {
      id
      name
      thumbnailUrl
      # location {
      #   formattedLocation
      # }
    }
  }
}
